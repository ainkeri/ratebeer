<% if session[:last_city].nil? %>
  <p>no places</p>
<% else %>
  <div>
    <h3><%= @place.name %></h3>
    <p><%= @place.status %><p>
    <p><%= @place.street %><p>
    <p><%= @place.zip %> <%= @place.city %><p>
  </div>
<% end %>